<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <style>
      @import url("https://fonts.googleapis.com/css2?family=Outfit&display=swap");
      * {
        margin: 0;
        padding: 0;
      }
      body {
        font-family: "Outfit", sans-serif;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
      }
      h1 {
        font-size: 3rem;
        font-weight: 900;
        margin: 1rem auto 0 auto;
        text-align: center;
      }
      i {
        font-size: 15px;
      }
      h2 {
        font-size: 1.5rem;
        margin: 0.5rem auto 0.5rem auto;
        font-weight: 400;
      }
      form {
        display: flex;
        justify-content: center;
        align-items: center;
        margin: 2rem auto 0 auto;
      }
      form button {
        cursor: pointer;
        color: white;
        font-weight: bolder;
        background-color: #b2533e;
        padding: 1rem 1rem 1rem 1rem;
        font-size: 1.2rem;
        border-radius: 1rem;
      }
      img {
        width: 50%;
        height: 5rem;
        object-fit: cover;
        min-width: 30rem;
      }
      .content {
        text-align: center;
        margin-top: 1.5rem;
        margin: 1.5rem 1rem 0 1rem;
      }

      .line {
        margin: 1rem auto 1rem auto;
        background-color: black;
        width: 50%;
        height: 1px;
      }
      .token {
        text-align: center;
        font-size: 1.3rem;
        font-weight: bold;
      }
      .token-value {
        margin-top: 1rem;
        border: 3px solid #9a4444;
        padding: 1rem;
        margin: 1rem auto 1rem auto;
        border-radius: 1rem;
        font-size: 1.2rem;
      }
    </style>
  </head>
  <body>
    <h1>Account Activation</h1>
    <i><%= time %></i>
    <div class="line"></div>
    <div class="content">
      <h2>Thank you <%= name %> for your registration to Cinemas Api</h2>
      <i class="token">Your Token :</i>
      <div class="token-value">
        <p><%= token %></p>
      </div>
      <h2>Before use it click this button to activated you accout</h2>
      <form action="<%= link  %>/auth/user/activated?id=<%= id %>" method="post">
        <button type="submit">Activate</button>
      </form>
    </div>
  </body>
</html>
